# FactoryDepo — Honesty & i18n audit (adversarial review of the RENDERED UI)

- Worktree: `C:/Users/Hp/FactoryDepo/.worktrees/design` (branch `design`) — **nothing modified, nothing committed**
- Environment: API `http://localhost:9095`, built SPA (vite preview) `http://localhost:5190`, standalone landing served from the worktree over `http://localhost:5233/artifacts/landing/variants/marketplace-light.html`
- Accounts: `demo@factorydepo.com` (admin), `buyer@factorydepo.com`, `supplier@factorydepo.com` — HMAC tokens minted locally (`audit/mint.mjs`) because `/api/auth/login` was rate-limited (`{"error":"rate_limited"}`); every token validated with `GET /api/me`
- Rendered with headless Chrome over CDP: `fd-batch.mjs` / `fd-audit.mjs` (both in the profile scratch dir) — full-page screenshots after a scroll walk, plus a DOM probe (innerText, raw-i18n-key detector, placeholder-text scan, `href="#"` scan, a11y gaps, KPI tiles, table text)
- **Language note:** the topbar selector is a `<select aria-label>`; the app's default language comes from `navigator.languages`, and this machine is `tr-TR`, so the pages below render **in Turkish by default** unless marked EN/AR. Language was switched via the app's own control (`select.value='tr'` → `change` → `localStorage['fd:lang']='tr'`, verified) and via `fd:lang` seeding for the EN/AR runs.
- **Drift warning:** the integration suite is running concurrently against the same dev DB (new `IT …`/`QA …` rows appear every few minutes), so catalogue totals move between snapshots (5,412 → 5,418; 133 → 134 RFQs). Every figure below is quoted with the API/DB value read at the same time.
- Audit window: 2026-09-22 19:38–19:58 (UTC+03:00)

Screenshots — copied into the worktree so the references survive scratch pruning (10 files, 5.4 MB):
`C:/Users/Hp/FactoryDepo/.worktrees/design/.omh/reviews/evidence/` → `product-guest.png` (TR product page), `product-ar.png` (Arabic fallback), `admin-overview.png`, `crop-suppliers-top.png` (supplier directory cropped to its KPI strip + first 8 cards; the uncropped 6 MB / 71,791 px-tall capture stays in the scratch dir), `supplier-detail.png`, `explore-guest.png`, `feed-buyer.png`, `notifications-buyer.png`, `sup-listings.png`, `landing-tr.png`.
Raw renders, `.txt` innerText dumps, `.dom.json` probes and `missing.json`: `C:/Users/Hp/AppData/Local/hermes/profiles/factory-depo/cache/scratch/audit/shots/`.

---

## Findings

| # | Sev | Page + route | What the user sees | Why it is wrong | Evidence | Source |
|---|-----|--------------|--------------------|-----------------|----------|--------|
| 1 | **BLOCKER** | `/suppliers` (public, guest) + `/suppliers/:id` | Supplier cards show `★★★★★ 21 denetim` (5 stars, "21 inspections") and a fact line `Puan 4.9 · Denetim 21 · Teslim performansı 96.8%`; the page strip says `4.8 Ortalama puan (382 puanlanmış)` and `97.1% Zamanında teslim (9 ölçülmüş)`. On `/suppliers/3`: `21 Yerinde denetim`, `96.8% Zamanında teslim`, `91 Güven puanı (0–100)`. For 8 suppliers the `Demo` tag is **absent** and the row reads `✓ Doğrulanmış` | No workflow produces these numbers. They are seed/import values presented as measured platform performance — including an on-site-inspection count, which is a physical claim. The app's own landing says the opposite: *"Ratings, inspection counts and fulfilment rates are not shown on this page: they are seeded values in our database and no workflow writes them yet."* `/supplier/verification` repeats it: *"Alıcılar diğer tedarikçilerin puanlarını ve denetim sayılarını da profillerinde görür. Bu rakamlar … pazar yerinin örnek verisidir — bu sayfa bilinçli olarak hiçbirini göstermez."* | DB: `rating>0` = 382/1,611 suppliers (all `dataSource='demo'`; **0** of the 66 `platform` rows), `inspectionsCount>0` = 9, `fulfillmentRate>0` = 9; 179 suppliers have `rating = 5.0` with 0 inspections; `Shandong Mining Group` = `demo`, rating 4.9, insp 21, fulf 96.8, level 3. Rendered: `audit/shots/suppliers-guest.txt:65-69`, `supplier-detail-shandong.txt:49-57`; landing copy `audit/shots/landing-en-true.txt:333`; screenshots `evidence/suppliers-guest.png`, `evidence/crop-suppliers-top.png`, `evidence/supplier-detail.png` | `artifacts/web/src/pages/Suppliers.tsx:16-60` (Totals), `:160-166` (per-card rating/inspections/fulfilment); `artifacts/web/src/components.tsx:665-668` (`Stars rating={s.rating}` + `{s.inspectionsCount} denetim`); `artifacts/web/src/pages/SupplierDetail.tsx` (trust panel); seed data `lib/db/scripts/seed.ts:86-94` |
| 2 | **BLOCKER** | `/suppliers` demo rows at level ≥ 2 | `✓ Doğrulanmış` badge with **no `Demo` tag** on rows that are seed data | AGENTS.md hard rule: *"Any row with `dataSource === 'demo'` must render `<DemoTag />`"*. The component renders `Verified` **instead of** `Demo` once `verifiedLevel >= 2`, so 8 demo suppliers are indistinguishable from verified real suppliers — while the platform's own verification desk reports `0 Onaylı` and `0 Gönderilen belgeler` | DB: `select "dataSource", count(*), count(*) filter (where "verifiedLevel">=2) from suppliers` → `demo | 1545 | 8`. Screenshot `evidence/crop-suppliers-top.png` (8 cards with `✓ Doğrulanmış` and no `Demo` chip; the rows below carry `Demo`) | `artifacts/web/src/components.tsx:660` — `{s.verifiedLevel >= 2 ? <Verified /> : (s.dataSource === 'demo' ? <DemoTag /> : null)}` |
| 3 | **BLOCKER** | `/rfqs` (buyer) + `/admin/rfqs` | RFQ row `Textile Yarn Polyester · 40 MT · Vietnam · 0 teklif · Kapalı` is listed with **Vietnam** as the delivery country, and **without** a Demo tag | Owner hard rule: the marketplace covers Türkiye, China, USA and Europe **only**; India and Vietnam were explicitly excluded. The row is `dataSource='platform'`, so it renders as real buyer demand | `curl /api/rfqs` → 133 rows, `Vietnam: 1`. DB `select id,title,"targetCountry","dataSource" from rfqs where "targetCountry" ~* 'vietnam\|india'` → `8 \| Textile Yarn Polyester \| Vietnam \| platform`. Rendered: `audit/shots/rfqs-buyer.txt:439`, `audit/shots/admin-rfqs.txt:705` | Seed `lib/db/scripts/seed.ts:195` (`targetCountry: 'Vietnam'`, no `dataSource`, so the column default `platform` applies) |
| 4 | **BLOCKER (latent)** | fresh DB / fallback landing | Vietnamese suppliers and India/Vietnam market names come back | The DB purge was done, but the code that creates them is still shipped | `lib/db/scripts/seed.ts:59,65,91,97` (`vietnam@minerals.vn`, `hanoi@packaging.vn`, both `country: 'Vietnam'`); `artifacts/api-server/src/bootstrap-seed.ts:38,70`; `artifacts/landing/index.html:1118,2312` and `artifacts/landing/variants/enterprise-dark.html:741` (`<span>Vietnam</span><span>India</span>` in the footer offices strip) — `index.html` is the **last fallback candidate** served at `/` when `marketplace-light.html` is missing (`artifacts/api-server/src/index.ts:124-139`, used at `:351-357`) | as listed |
| 5 | **MAJOR** | `/explore` (guest) and `/feed` (buyer) | The first page (24 cards) is **half** test fixtures: 6 × `IT surplus coil lot · Türkiye · MOQ 1 t · $120 / t` and 6 × `IT oversell probe mucwhwvs9e9ddi · Türkiye · MOQ 1 MT · $10 / MT`, none with a Demo tag. `/admin` KPI reads `✅ 16 Gerçek ilanlar` ("16 real listings") | These are throwaway rows created by the API integration suite (`*@factorydepo.test` accounts); they are the **entire** `platform` row set, so the admin console's "real listings" figure counts test junk, and a guest browsing the catalogue sees plausible-looking real offers that no supplier made | Rendered: `audit/shots/explore-guest.txt:112-213` (12 test cards / 24 = counts of `IT surplus coil lot` ×6 + `IT oversell probe` ×6, vs 12 `Demo` tags); `audit/shots/feed-buyer.txt:127-253`; DB: all 16 `dataSource='platform'` products → `IT oversell probe …` / `IT surplus coil lot`, suppliers `IT … Ltd`, emails `it-supplier-…@factorydepo.test`; screenshots `evidence/explore-guest.png`, `evidence/feed-buyer.png` | Tests `artifacts/api-server/test/api.test.ts:228` (`IT oversell probe`), `:307` (`IT surplus coil lot`), `:53`; the cleanup that should follow a local run was not applied — `scripts/cleanup-test-rows.sql` |
| 6 | **MAJOR** | `/products/:id` | Badge strip on the listing: `✓ Doğrulanmış · Aktif · Demo · ✓ Doğrulanmış`, seller box `SINOCEAN SUZHOU Ltd ✓ Doğrulanmış Demo · CN · Seviye 1`, and a trust row `✓ Doğrulanmış tedarikçi` | `Verified` is driven by `products.verified`, which is `true` on 5,397 rows — including 5,370 demo rows whose supplier sits at **verifiedLevel 1**. The same codebase refuses to render `Verified` for a supplier below level 2 (`ProductDetail.tsx:460`, `components.tsx:660`), so the product-level badge contradicts the app's own bar and asserts a review that never happened | `curl /api/products/5412` → `"verified": true`, `"dataSource": "demo"`, supplier `verifiedLevel: 1`; DB join: `demo + verified=t + supplier level 1` = 5,370 rows; `products.verified` distribution `t:5397 / f:21`. Screenshot `evidence/product-guest.png` (TR) | `artifacts/web/src/pages/ProductDetail.tsx:708`, `:728`, `:884`, `:903` — `{p.verified && <Verified />}` with no `dataSource`/level gate; also `artifacts/web/src/components.tsx:618` (product card `vtag`) |
| 7 | **MAJOR** | `/products/:id` | Seller card always shows `🔍 Denetlenmiş stok` / `Ödeme öncesi fabrika denetimi` ("Inspected stock / factory inspection before payment") | The row is rendered unconditionally, so every listing — including ones whose supplier has `inspectionsCount = 0` and `verifiedLevel = 1` — claims inspected stock. It also sits next to the product badges, where it reads as a property of this lot rather than a service the platform offers | `curl /api/suppliers/1572` → `inspectionsCount: 0`, `verifiedLevel: 1`; rendered `audit/shots/product-guest.txt:86-88` | `artifacts/web/src/pages/ProductDetail.tsx:912-918` (trustrow is not guarded, unlike the `pd.trustVerified` row directly above it at `:903-911`) |
| 8 | **MAJOR** | `/suppliers/:id` (public, signed out) | The company profile prints the importer's internal note verbatim: `Imported from made-in-china.com — SINOCEAN SUZHOU Ltd. Store: https://14ac0f882ffb54ca.en.made-in-china.com. Contact via platform (email/phone are not publicly listed).` | A buyer-facing page leaks the ingestion provenance and the source storefront URL, and it contradicts the admin Sources screen on the same product: *"Hiçbir dizin içe aktarılmaz ve hiçbir katalog kazınmaz"* (no directory is imported, no catalogue is scraped) — while 929 suppliers carry `source = 'made-in-china.com'` and the same "Imported from …" description | `curl /api/suppliers/1572` → description starts `Imported from made-in-china.com —`; DB: `count(*) filter (where description like 'Imported from made-in-china.com%')` = 929, `with_source` = 929; rendered `audit/shots/supplier-detail.txt:63-65`; admin claim `audit/shots/admin-sources.txt:31-38`. Screenshot `evidence/supplier-detail.png` | `artifacts/web/src/pages/SupplierDetail.tsx:115` (`{s.description ?? …}`), same pattern at `artifacts/web/src/pages/ProductDetail.tsx:488-490`; data written by the importer into `suppliers.description` |
| 9 | **MAJOR** | every page (topbar → sidebar) | Sidebar user block shows hardcoded English under the name: admin → `Full access`, supplier → `Supplier account`, buyer → `Buyer account` — in the Turkish UI | The string is not in the dictionary at all, so it is untranslated in all six languages | `audit/shots/admin-overview.txt:21` (`Full access`), `audit/shots/sup-listings.txt:21` (`Supplier account`), `audit/shots/feed-buyer.txt:48` / `orders-buyer.txt` / `rfqs-buyer.txt` (`Buyer account`) — all inside otherwise-Turkish pages | `artifacts/web/src/App.tsx:95` — `meta: dash === 'supplier' ? 'Supplier account' : dash === 'admin' ? 'Full access' : 'Buyer account'` |
| 10 | **MAJOR** | `/products/:id` + dashboards in `ar` `ru` `zh` `es` | Choosing العربية (or Русский/中文/Español) in the topbar yields Arabic chrome with an English product page: `Home ›`, `Hover to zoom`, `Product images · 1 photos`, `Key facts`, `UNIT PRICE`, `LISTED ON`, `Listed 43 days ago`, `Not provided`, `Order this stock` … and an English dashboard | Four of the six languages the switcher offers each miss the same 131 keys — 87 `pd.*` (product detail) and 44 `dash.*` (dashboards), i.e. exactly those two namespaces — and `t()` silently falls back to English, so the page looks half-translated with no indication | Key diff over the dictionaries: `en` 1,690 keys, `tr` 1,690 (complete), `ar`/`ru`/`zh`/`es` 1,559 each; missing set = 87 × `pd.*` + 44 × `dash.*` (`audit/missing.json`). Rendered proof `audit/shots/product-ar.txt` (`Home`, `Hover to zoom`, `Product images · 1 photos`, `Key facts`, `UNIT PRICE`, `LISTED ON`, `Not provided`, `Order this stock`); screenshot `evidence/product-ar.png` | `artifacts/web/src/i18n.tsx` — `const ar` (line 5468), `const ru` (7096), `const zh` (8721), `const es` (10319); fallback behaviour documented at the file header (`:14-17`) |
| 11 | **MAJOR** | `/notifications` (buyer, TR) | Notification bodies and type chips stay English inside the Turkish UI: `message / New message from Mehmet Demir.`, `shipment / Order #73 was delivered.`, `payment / Payment of USD 12250 recorded for order #72 — awaiting confirmation.`, `offer / Offer #10 was rejected.` | Buyer-facing notifications are written server-side in English and the type is rendered as the raw enum value (`message`, `shipment`, `payment`, `offer`), so a Turkish-speaking buyer gets untranslated system text and internal-looking labels | `audit/shots/notifications-buyer.txt:56-96`; screenshot `evidence/notifications-buyer.png` | API notification writer (English strings queued in `notifications`); UI `artifacts/web/src/pages/Notifications.tsx` renders the API body verbatim and the raw `type` value |
| 12 | **MAJOR** | `/categories` (TR) | Category tiles keep English blurbs under the Turkish heading: `Machinery — Production machinery and machine tools`, `Steel — Coil, sheet, sections and structure`, … (all 25) | The blurb is data-baked into the client, not in the i18n dictionary, so it never translates | `audit/shots/categories-guest.txt:51-125` vs `audit/shots/categories-en.txt` (identical lines) | `artifacts/web/src/categoryImages.ts:27-31+` (`blurb:`), rendered by `artifacts/web/src/components.tsx:505-508` (`{cover?.blurb ?? t('categories.stockHere')}`) |
| 13 | MINOR | `/` (landing, hero stat strip) | `5,175 LIVE LISTINGS` | That number is the **photo-bearing** listing count (`/api/products?hasImage=1`), not the catalogue total (5,412 at the same moment). The same page labels the identical figure correctly further down ("Showing 8 of 5,175 **lots that have photography**"), so the hero tile understates and mislabels the catalogue | `curl '/api/products?limit=1'` → `total 5412`; `curl '/api/products?hasImage=1&limit=1'` → `total 5175`; rendered `audit/shots/landing-en-true.txt:38-39` (`5,175` / `LIVE LISTINGS`) vs `:87` (the correctly-labelled surplus paragraph) | `artifacts/landing/variants/marketplace-light.html:3285` + `:3302` (`counters.products = res.total` of `hasImage=1`) rendered into the tile defined at `:486` with label `stats.lots` = "Live listings" (`:2167`) |
| 14 | MINOR | `/` (landing topbar) | `Markets: Türkiye · China · Global` | Hardcoded, and it omits two markets the platform does serve (USA, Europe) while listing "Global" (not a country, and deliberately not flagged elsewhere). Under-states the marketplace's own scope on the first screen | Rendered `audit/shots/landing-en-true.txt:5-7`; the API's real country spread is `CN 4565 · Global 821 · TR 23 · DE 3` (`curl /api/products/countries`) | `artifacts/landing/variants/marketplace-light.html:424-427` (markup) and `:870-873` (dictionary) — literal spans, not data |
| 15 | MINOR | `/supplier/listings` (TR), "Yayındaki gün sayısı" column | The posted-date cell shows the bare words `Yayındaki gün sayısı` (EN: `Days live`) where a number belongs — every row | The translation lost the `{n}` placeholder, so the figure is silently dropped and the cell renders as a stray label | Rendered `audit/shots/sup-listings.txt:100,112-113`; screenshot `evidence/sup-listings.png` | `artifacts/web/src/i18n.tsx:3686` (`'dash.daysLive': 'Days live'`) and `:5459` (`'Yayındaki gün sayısı'`) vs call site `artifacts/web/src/pages/SupplierListings.tsx:644` (`t('dash.daysLive', { n: days.toLocaleString(locale) })`) |
| 16 | MINOR | `/products/:id` | `Product images · 1 photos` | No plural form in the string, and the count is hardcoded to 1 (the API exposes a single `imageKey`) | Rendered `audit/shots/product-en.txt:37` | `artifacts/web/src/i18n.tsx:3564` (`'pd.photoCount': '{n} photos'`); `artifacts/web/src/pages/ProductDetail.tsx:744` (`{ n: 1 }`) |
| 17 | MINOR | topbar (all signed-in pages) + `/admin` | A grey chip next to the user name renders the raw role enum lowercase: `admin`, `buyer`, `supplier`; the admin overlay also prints `demo@… / admin` | Raw internal values in the chrome, untranslated. Cosmetic but it is the kind of token the UI rules ask to keep out of the interface | `audit/shots/admin-overview.txt:16-21`, `feed-buyer.txt:16-19`, `sup-listings.txt:16-21` | `artifacts/web/src/components.tsx:297` (`<span className="pill p-navy">{role}</span>`) |
| 18 | MINOR | `/admin/suppliers`, `/suppliers`, `/admin/sources` | One page render = 1,612 table rows / 3,227 buttons / **91,612 px** tall (`/admin/suppliers`); 1,611 cards / **72,188 px** (`/suppliers`); `/admin/sources` 84,497 px. No pager, no virtualisation | Whole-table renders of the full supplier base on every visit; the public directory is unusable on a phone and the admin screen freezes the tab while it lays out | `fd-check.mjs` reports: `document.body.scrollHeight` = 91612, `table tr` = 1612, `button` = 3227 (`/admin/suppliers`); `scrollHeight` = 72188, `a.card` = 1611 (`/suppliers`); full-page captures `1418 x 71791 px` (suppliers) / `1418 x 84497 px` (admin sources) / `1418 x 91612 px` (admin suppliers) in `C:/Users/Hp/AppData/Local/hermes/profiles/factory-depo/cache/scratch/audit/shots/` (`file *.png`); `evidence/admin-overview.png` shows the same console for scale | API ignores `limit`/`page` on the supplier list (returns everything); the pages map over the full array — `artifacts/web/src/pages/AdminSuppliers.tsx`, `artifacts/web/src/pages/Suppliers.tsx` |
| 19 | MINOR | `/admin` KPI `Onaylanan ödemeler · toplam` | Renders `12.250` with no currency symbol | The value is `sum(amount)` across all payment rows with no currency grouping; today only one USD row exists (`USD | confirmed | 1 | 12250.00`) so the figure is right **by luck**. A EUR/TRY row would silently add into a meaningless total, and the tile gives the reader no unit | `curl /api/admin/overview` → `"paymentsConfirmedTotal": 12250`; DB `select currency,status,count(*),sum(amount) from payments group by 1,2` → single row `USD | confirmed | 1 | 12250.00`; rendered `audit/shots/admin-overview.txt:99-100` | `artifacts/api-server/src/routes/admin.ts:125` (`sum("amount") … WHERE "status"='confirmed'`); label `admin.overview.paymentsConfirmedTotal` |
| 20 | NIT | `/offers` (buyer) | Quantity cell reads `Başlangıçta 1.` (EN `Originally 1.`) | Sentence fragment — the unit is missing and the full stop dangles | Rendered `audit/shots/offers-buyer.txt`, `audit/shots/offers-en.txt:95` | `artifacts/web/src/i18n.tsx:2748` / `:4520` (`'myoffers.originally': 'Originally {qty}.'`) |
| 21 | NIT | `/admin` sidebar | `🚚` is used for both `Tedarikçiler` (Suppliers) and `Sevkiyatlar` (Shipments) | Two different destinations share one glyph; decorative only (both are inside labelled links) | `audit/shots/admin-overview.txt:26-41` | `artifacts/web/src/components.tsx` `NAV_ADMIN` |
| 22 | NIT | `/products/:id` (photo) | The ice-machine listing shows a servo-drive/controller photograph, captioned `Ürün görselleri · 1 fotoğraf` | The catalogue shares 38 FactoryDepo-owned images across all 5,402 rows, so a listing's "product photo" is frequently an unrelated machine. Mitigated by the `Demo` tag on the row | `curl /api/products/5412` → `"imageKey": "/products/servo-drive.jpg"`; screenshot `evidence/product-guest.png` | `artifacts/web/public/products/` (shared image pool); rendered at `artifacts/web/src/pages/ProductDetail.tsx` gallery |

---

## Verified clean (checked, found honest)

1. **No forbidden content anywhere in the rendered UI.** Scan of all 30 rendered pages (EN + TR + AR) for `hermes`, `gpt`, `openai`, `claude`, `llm`, `vibe`, `ai-generated`, `copilot`, `prompt`: **0 hits**. The only `test`-like matches are the legitimate service name `Laboratuvar testi` (laboratory testing).
2. **No placeholder text** (`Lorem`, `TODO`, `FIXME`, `asdf`, `foobar`, `dummy`, `xxx`) in any rendered page. The single hit was inside a `<script>` source comment on the landing, never painted.
3. **No `href="#"` links, no `<a>` without `href`, no icon-only button without an accessible name, no form field without a label/`aria-label`, no `<img>` missing `alt`** on any audited page (probe fields `hashLinks`, `emptyLinks`, `iconButtonsNoName`, `inputsNoLabel`, `imagesWithoutAlt` are 0 across all pages).
4. **`<img alt="">` on category tiles is correct, not a defect** — the tile's link text carries the category name (`components.tsx:498-510`), so the image is decorative. 25 such tiles on `/categories`.
5. **The landing's demo notice renders properly** (no literal `<b>` markup leaking into the text): `Demo catalogue — every listing shown is bootstrap demo data, not a real offer.`
6. **Product key facts all reconcile with `GET /api/products/5412`:** `$6,500` = `price`, `unit` = `unit`, `1 unit` = `moq`, `100 unit` = `quantityAvailable`, `Hazır stok` = `listingType: stock`, `CN` = `originCountry`, `Food Processing` = `category`, `09.08.2026` = `createdAt` (43 days ago — floor of elapsed), `SAFLIK / KALİTE → Belirtilmemiş` for `purityGrade: null`, and the line total `Satır toplamı · 1 unit → $6,500` = qty × unit price.
7. **Money arithmetic on the order/stock tiles is correct:** buyer `/orders` → `$66,580` = Σ of its 6 order rows and `$11,096.67` = 66,580 / 6; supplier `/supplier/listings` → `$16,487,250` = Σ(price × quantityAvailable) over its 7 rows; buyer `/saved` → `$262,000` = Σ over the 3 saved rows (48,000 + 171,000 + 43,000).
8. **Landing live counters match the API at page-load time:** `1,611 SUPPLIERS` = `/api/suppliers.total`; `25 CATEGORIES` = length of `/api/products/categories`; `3 COUNTRIES` = 3 distinct two-letter supplier country codes, with the derivation rule in the element `title` (*"Distinct two-letter country codes on supplier profiles"*); RFQ rail `showing 5 / 133` = `/api/rfqs.total`; every RFQ card carries its status from the API.
9. **Admin overview KPIs are all real counts** — every one of the 20 tiles matched `GET /api/admin/overview` field-for-field (`users 1697, buyers 83, suppliers 1611, products 5418, demoProducts 5402, platformProducts 16, rfqs 133, quotes 9, orders 40, offers 4, openOffers 2, threads 3, messages 5, shipments 38, paymentsAwaiting 0, docsAwaitingReview 0, supportTicketsOpen 0, emailsQueued 171`).
10. **The Demo tag is present where the rules require it** on: product cards (`/explore`, `/feed`, `/products/:id`, saved, supplier rails), RFQ cards, the landing cards, the admin `Başlangıç ilanları` tile, and the pagers' size line.
11. **Honest empty/unknown states are used consistently:** `—` for `purityGrade`, rating, inspections, fulfilment, `since`, trust score (`/suppliers/1572` shows `—` and "Alıcı puanı — henüz puanlanmadı"), genuine `0` values printed as `0` (e.g. `0 Onay bekleyen ödemeler`), and the category tile *omits* the count instead of printing a zero it did not measure (`components.tsx:509-511`).
12. **No India/Vietnam anywhere in the served landing or the browse surfaces**: `marketplace-light.html` has no India/Vietnam; the product country rail is `🇹🇷 27 · 🇨🇳 4565 · 🇺🇸 0 · 🇩🇪 3 · 🇳🇱 0 · 🇮🇹 0`; the `/explore` country filter lists `CN/Global/TR/DE`; `lib/api-spec/src/index.ts:146-149` `COUNTRIES` is clean. (The Vietnam defect is the RFQ row in finding 3.)
13. **The product Q&A tab is genuinely wired**: clicking it renders `Soru ve cevaplar` + the honest empty state `Bu ilan için henüz soru yok.` + `Satıcıya soru sormak için oturum açın` (rendered DOM read after the click).
14. **Guest-only actions really gate**: clicking `Satıcıyla iletişime geç` on `/products/5412` signed out opens the auth modal (`.overlay .modal` → `Üye erişimi — Tedarikçilerle iletişim, talep yayınlama ve teklif verme üyelere özeldir…`), not a dead control.
15. **The language switcher works and is labelled**: `select[aria-label="Dil"]` → changing the value writes `localStorage['fd:lang']='tr'` and re-renders (`document.documentElement.lang === 'tr'`).
16. **Turkish is 100% complete**: `tr` has all 1,690 `en` keys (no raw keys and no English fallback inside the Turkish dictionary itself — the untranslated strings reported above are hardcoded literals, API strings, or seed data, all traced to their source).
17. **Admin screens state their own limits instead of inventing data**: `/admin/payments` ("Bir ödemeyi onaylamak … tek bir işlem ve bu ekrandan geri alınamaz"), `/admin/features` ("Bu API henüz hiçbir anahtar sunmuyor"), `/admin/growth` ("Afiş güncelleme uç noktası olmadığından …"), `/supplier/verification` ("bu sayfa bilinçli olarak hiçbirini göstermez"), `/supplier/post` ("Bu sayfa görüntüleme, puan veya sipariş sayısı göstermez").
18. **No fabricated percentage without a denominator** in KPI form: the only percentages on the audited pages are either product data (`60% Sb`, `99.995%`) or the seed fulfilment rates already reported in finding 1; the fulfilment tile carries its denominator in the label (`382 puanlanmış`, `9 ölçülmüş`).

---

## Reproduction commands

```bash
# tokens (login is rate-limited: 10/15min/IP)
node C:/Users/Hp/AppData/Local/hermes/profiles/factory-depo/cache/scratch/audit/mint.mjs

# render a page and dump DOM evidence (innerText + probe json + screenshot)
cd C:/Users/Hp/AppData/Local/hermes/profiles/factory-depo/cache/scratch
node fd-batch.mjs --out=audit/shots --jobs='[{"name":"suppliers","url":"http://localhost:5190/suppliers"}]'

# interaction probes
node fd-check.mjs "http://localhost:5190/products/5412" --w=1440 --h=1000 \
  --steps='[{"wait":5500},{"clickText":"button|Satıcıyla iletişime geç"},{"wait":900},
            {"report":"document.querySelector(\".overlay .modal\")?.innerText.slice(0,200)"}]'

# data checks
export PGPASSWORD=postgres
"/c/Program Files/PostgreSQL/16/bin/psql.exe" -h localhost -U postgres -d factorydepo \
  -f C:/Users/Hp/AppData/Local/hermes/profiles/factory-depo/cache/scratch/audit/q1.sql
curl -s http://localhost:9095/api/admin/overview -H "Authorization: Bearer $(cat audit/admin.token)"
curl -s 'http://localhost:9095/api/products?hasImage=1&limit=1'   # -> {"total":5175,…}
```

**Attack surface I did not cover** (stated so the audit is not read as complete): the mobile feed's device-width behaviour (not part of this task's criteria), the `ar/ru/zh/es` dictionaries' *quality* (only completeness), the landing's `file://` render path, keyboard tab-order walk-through (only static a11y attributes were probed), and any prod-only surface — everything above is the local dev stack.
